const Header = () => {
  return (
    <header>
      <h1>Median Prime</h1>
      <p>
        {`Use the form below to calculate the median prime number or numbers from the range of all prime numbers between 0 and your given number.`}
      </p>
    </header>
  );
};

export default Header;
